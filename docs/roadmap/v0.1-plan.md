# Milestone v0.1 — Parser, AST, Formatter (Implementation Plan)

Goal: Deliver a deterministic parser, concrete AST, and canonical formatter that normalizes source code and enables `aic check` (format-or-fail) for the MVP surface described in docs/syntax.md.

## Scope & Outcomes
- Parse the MVP language surface: modules, imports, structs, enums, type aliases, consts, lets, functions (incl. async, generics, where, effects), traits, impls, control flow (`if/else`, `loop`, `for`), `match`, basic expressions (calls, member access, literals, `await`, unary/binary ops), patterns, and blocks.
- Produce a stable, lossless AST with source spans and doc comments.
- Implement a canonical, idempotent formatter. `aic fmt` rewrites; `aic check` fails on differences.
- Emit machine-readable diagnostics (JSON) with stable codes and 1-step fix-its for common parse/format errors.
- Establish token-economy baselines and formatting rules that minimize tokens without ambiguity.

## Deliverables
- Lexer, Parser, AST modules with unit tests and golden tests.
- Formatter with style rules implemented and idempotency tests.
- `aic fmt` and `aic check` subcommands wired to frontend.
- Diagnostic schema and initial error codes (E0001–E0015).
- Golden examples: all snippets from docs/vision.md and docs/syntax.md parse and format cleanly.
- Metrics script: collect token/char counts before/after formatting on sample corpus.

## Non-Goals (v0.1)
- No type checking, trait resolution, effects checking, or codegen.
- No macro system, operator overloading, or semicolon elision.

## Language Surface (MVP Parse Set)
- Files: UTF-8, newline `\n`; `//`, `/* */`, `///` comments; comments attach to nearest following item.
- Decls: `module path.name;`, `use path::item [as alias];`, `pub` visibility; `struct`, `enum`, `type`, `const`, `let`.
- Fns: `pub? async? fn name<T...>(params) -> Ret where ... effects(...)? { ... }`.
- Traits/impls: `trait` with method sigs; `impl Trait for Type { ... }`.
- Control: `if/else`, `loop`, `for x in iter {}`, `match expr { arms }`.
- Expr: literals, tuples, arrays, maps, calls, field/index access, `await`, unary/binary ops, blocks, `return`, `?`.
- Patterns: identifiers, `_`, destructuring for tuples/structs/enums.
- Semicolons required after statements; K&R braces; 4-space indents; trailing commas allowed in multiline lists.

## Grammar (Sketch, EBNF-style, excerpt)
- module_decl = "module", path, ";";
- use_decl = "use", path, ["as", ident], ";";
- item = module_decl | use_decl | struct_decl | enum_decl | type_alias | const_decl | fn_item | trait_decl | impl_block | doc_comment;
- fn_item = vis?, async?, "fn", ident, generics?, params, ret_type?, where_clause?, effects_clause?, block;
- effects_clause = "effects", "(", ident_list, ")";
- block = "{", stmt*, "}";
- stmt = let_stmt | expr_stmt | item | "return", expr?, ";";
- match_expr = "match", expr, "{", match_arm+, "}"; match_arm = pattern_list, "=>", expr_or_block, ",";
- pattern = "_" | ident | struct_pat | enum_pat | tuple_pat | lit;

Full grammar to be maintained as a source file (grammar/README.md) and mirrored in docs/syntax.md.

## AST Design
- Lossless for formatting: retain tokens necessary for canonical layout decisions; preserve doc comments (`///`) and item-level `//` when leading.
- Nodes carry: kind, children, stable NodeId, source span (file, start, end), doc string (optional), visibility, attributes (reserved).
- Keep expressions and patterns distinct; uniform literal nodes; explicit `Effects` list on functions.
- No semantic fields (types/resolution) yet; names are plain idents and paths.

## Lexer & Tokens
- Recognize keywords from docs/syntax.md; identifiers; numeric literals (int, float, hex); string/bytes; punctuation; operators.
- Handle raw strings `r"..."` and escape sequences with strict validation.
- Tokenize `_` as an identifier but allow pattern/expr hole handling in parser with context.
- Track trivia (whitespace/comments) in a side channel for doc comment association.

## Error Recovery Strategy
- Panic-mode with synchronization tokens: `;`, `}`, `]`, `)`, `,`.
- Target single-root-cause diagnostics: stop cascades after first confident error in a region.
- Common fix-its: insert missing `;`, insert missing `,`, add `}`/`)`, replace unexpected token, suggest `use` for FQNs (formatting stage only).

## Formatter Design
- Idempotent: format(parse(format(src))) == format(src).
- Statement layout: one per line; required `;`.
- Braces: same-line opens; `} else {` compact form.
- Imports: group `use` after `module`; sort by path; one per line; preserve `as alias`.
- Spaces: operators have single spaces around, none before `;`, none before `,`.
- Indentation: 4 spaces; continued lines align to 1 indent.
- Trailing commas: enabled for multiline collections and enum/struct lists.
- Blank lines: one between top-level items; none at file end; keep doc comments attached.
- Strings: do not normalize escapes (avoid semantic changes); preserve raw vs normal.

## Diagnostics (Initial Codes)
- E0001: Unexpected token
- E0002: Expected token missing (detail)
- E0003: Unclosed delimiter (which)
- E0004: Invalid numeric literal
- E0005: Invalid string/escape
- E0006: Trailing tokens after item
- E0007: Duplicate module declaration
- E0008: Misplaced `effects(...)`
- E0009: Invalid match arm (missing comma)
- E0010: Invalid pattern
- E0011: Reserved keyword used as ident
- E0012: File encoding error
- E0013: Doc comment placement error
- E0014: Formatter disagreement
- E0015: Idempotency failure

Each diagnostic includes span, message, and optional fix-it with replacement text.

## CLI & JSON Output
- `aic fmt [--check] [--json] [paths...]`
  - Writes formatted code unless `--check` is used; exits 0 if clean, 2 if disagreements.
- `aic check [--json] [paths...]`
  - Parses + formats in-memory; exits non-zero on parse errors or format disagreements; prints diagnostics.
- JSON schema versioned; include file, line/col, code, message, notes, fix-its.

## Tests & Quality Gates
- Golden files: parse and format all examples from docs; store formatted output as goldens.
- Idempotency property tests for formatter.
- Fuzzing: token-level mutator to feed parser; assert no panics; errors are reported.
- Import ordering tests; block/brace spacing tests.
- Token budget report: script prints token/char counts for examples pre/post formatting.

## Current Implementation State

The project has foundational infrastructure in place (~10-15% of v0.1 scope):

| Area | Status | Detail |
|------|--------|--------|
| **Lexer** | **Done** | All operators, keywords, hex/raw-string/bytes literals, trivia side channel; full test suite |
| **AST** | **Done** | All v0.1 node types: declarations, functions, traits/impls, expressions, patterns, control flow |
| **Parser** | Partial | `module`, `use`, `struct`, `enum`, `type alias`, `const` declarations with doc attachment and error recovery |
| **Formatter** | Minimal | Formats module line + sorted use lines only |
| **Diagnostics** | Partial | E0001, E0002, E0014 emitted; schema/JSON output working |
| **CLI** | Wired | `aic fmt` and `aic check` functional for current parser scope |
| **Tests** | Partial | `tests/test_lexer.py` and `tests/test_parser.py` exist; no AST/formatter tests yet |
| **Grammar doc** | None | `grammar/README.md` does not exist |

## Implementation Steps (Dependency-Ordered)

Steps are ordered by dependencies. Each step is independently scoped and testable, suitable for an individual openspec change.

### ~~Step 1 — Lexer Completion~~ ✓ DONE

**Depends on**: Nothing (foundation)

Extend `aiclang/token.py` and `aiclang/lexer.py`:
- Add missing operators: `<`, `>`, `<=`, `>=`, `==`, `!=`, `&&`, `||`, `|`, `->`
- Add missing keywords: `mut`, `break`, `continue`, `shadow`, `type`
- Add hex integer literals (`0xFF`)
- Add bytes literals (`b"..."`)
- Add raw string literals (`r"..."`)
- Implement trivia tracking (whitespace/comments in side channel) for doc comment association
- Add lexer unit tests covering all token kinds, edge cases, error tokens

### ~~Step 2 — AST Node Scaffold~~ ✓ DONE

**Depends on**: Step 1 (uses `Span`/`Token` types)

Expand `aiclang/ast.py` with all v0.1 node types:
- Type declarations: `StructDecl`, `EnumDecl`, `TypeAlias`, `ConstDecl`
- Bindings: `LetStmt`
- Functions: `FnItem` (with `GenericParam`, `Param`, `WhereClause`, `EffectsClause`)
- Traits/Impls: `TraitDecl`, `ImplBlock`
- Expressions: `LiteralExpr`, `IdentExpr`, `PathExpr`, `BinaryExpr`, `UnaryExpr`, `CallExpr`, `MemberExpr`, `IndexExpr`, `AwaitExpr`, `BlockExpr`, `ReturnExpr`, `TryExpr` (`?`)
- Patterns: `WildcardPat`, `IdentPat`, `LiteralPat`, `TuplePat`, `StructPat`, `EnumPat`
- Control flow: `IfExpr`, `LoopStmt`, `ForStmt`, `MatchExpr`, `MatchArm`

### ~~Step 3 — Parser: Type Declarations~~ ✓ DONE

**Depends on**: Steps 1–2

Extend `aiclang/parser.py` to parse:
- `pub? struct Name { field: Type; ... }`
- `pub? enum Name { Variant(Type); ... }`
- `pub? type Name = Type;`
- `pub? const NAME: Type = expr;`
- Visibility (`pub`) prefix handling
- Doc comment attachment to following item
- Basic error recovery: synchronize on `}` and `;`

### Step 4 — Parser: Function Signatures and Let Bindings

**Depends on**: Step 3

Parse:
- `pub? async? fn name<T>(params) -> RetType where T: Bound effects(cap) { body }`
  - Generics: `<T, U>` parameter lists
  - Params: `name: Type` with `,` separators
  - Return type: `-> Type`
  - Where clause: `where T: Trait`
  - Effects clause: `effects(io, net)`
- `let mut? name: Type = expr;`
- Function body as block: `{ stmt* }` (initially statements are let/const/return with literal/ident expressions)

### Step 5 — Parser: Expressions with Precedence

**Depends on**: Step 4

Implement Pratt (precedence-climbing) expression parser:
- Atoms: integer, float, string, bool, `none`, identifiers, paths (`a::b::c`), parenthesized `(expr)`, `vec![...]` array literals
- Unary prefix: `!`, `-`
- Binary (low→high): `||`, `&&`, `== != < <= > >=`, `+ -`, `* / %`
- Postfix: `.field`, `.method(args)`, `[index]`, `(args)` call, `?`, `await`
- `return expr`
- Block expressions `{ ... }`
- Wire expression parsing into let/const initializers, function bodies, and const declarations

### Step 6 — Parser: Control Flow and Match

**Depends on**: Step 5

Parse:
- `if cond { ... } else { ... }` chains
- `loop { ... }` with `break`/`continue`
- `for name in expr { ... }`
- `match expr { pattern => expr_or_block, ... }` with required exhaustiveness syntax
- Pattern parsing: `_`, identifier binding, literal, `Variant(pat)`, `Struct { field: pat }`, `(pat, pat)`, nested patterns

### Step 7 — Parser: Traits and Impls

**Depends on**: Steps 4–5

Parse:
- `pub? trait Name { fn method_sig(...) -> Type; ... }`
- `impl Trait for Type { fn method(...) { body } ... }`
- Reuse fn signature and expression parsing from steps 4–5
- Support `&self` parameter in method signatures

### Step 8 — Parser: Error Recovery and Diagnostics

**Depends on**: Steps 3–7 (all parser steps)

- Implement panic-mode synchronization on `;`, `}`, `]`, `)`, `,`
- Add diagnostic codes E0003–E0013 with fix-its:
  - E0003: Insert missing closing delimiter
  - E0004: Flag invalid numeric literal
  - E0005: Flag invalid string/escape
  - E0006: Trailing tokens after item
  - E0007: Duplicate module declaration
  - E0008: Misplaced `effects(...)` 
  - E0009: Missing comma in match arm
  - E0010: Invalid pattern
  - E0011: Reserved keyword used as identifier
  - E0012: File encoding error
  - E0013: Doc comment not attached to item
- Enforce single-root-cause policy: suppress cascading errors after first confident error in a region

### Step 9 — Formatter: Declarations

**Depends on**: Steps 2–3

Extend `aiclang/formatter.py` to format:
- Struct declarations: K&R braces, 4-space indent, field semicolons, trailing commas in multiline
- Enum declarations: variant layout, inline vs multiline
- Type aliases, const declarations
- One blank line between top-level items
- Doc comment attachment (no blank line between doc comment and item)

### Step 10 — Formatter: Functions, Traits, Impls

**Depends on**: Steps 4, 7, 9

Format:
- Function signatures: line breaking for long signatures, consistent param layout
- Trait declarations and impl blocks
- Method bodies with proper indentation
- Operator spacing around `=`, `:`, `->`, `,`
- Semicolons after statements, no space before `;`

### Step 11 — Formatter: Expressions and Control Flow

**Depends on**: Steps 5–6, 10

Format:
- Binary/unary expressions: single spaces around operators, no gratuitous parentheses
- If/else: `} else {` compact form on same line
- Loop/for blocks: standard indentation
- Match expressions: arm indentation, trailing commas, `=>` alignment
- Nested blocks and expression statements

### Step 12 — Formatter: Idempotency and Polish

**Depends on**: Steps 9–11

- Verify `format(parse(format(src))) == format(src)` via property tests
- Finalize blank-line rules between items, within blocks
- Import sorting edge cases (aliases, re-exports)
- String preservation: no escape normalization, preserve raw vs normal
- E0014 (formatter disagreement) and E0015 (idempotency failure) full coverage

### Step 13 — Golden Tests and Acceptance

**Depends on**: All above

- Parse and format every example from `docs/syntax.md` and `docs/vision.md`
- Store formatted output as golden files
- Validate JSON diagnostics against schema
- `aic check` and `aic fmt` pass on all examples
- Record token/char metrics baseline
- Fuzzing pass: token-level mutator feeds parser; assert no panics

### Step 14 — Grammar Documentation

**Can proceed in parallel with**: Steps 3–7

- Create `grammar/README.md` with formal EBNF mirroring implemented parser
- Cross-reference `docs/syntax.md`
- Ensure grammar and parser agree on all productions

## Dependency Graph

```
1 (Lexer) ──► 2 (AST) ──► 3 (Decls) ──► 4 (Fns/Let) ──► 5 (Exprs) ──► 6 (Control/Match)
                                │                │                            │
                                ▼                ▼                            ▼
                           9 (Fmt:Decl)    7 (Traits) ──────────────► 10 (Fmt:Fn/Trait)
                                │              │                            │
                                ▼              ▼                            ▼
                           8 (Recovery) ◄── all parser          11 (Fmt:Expr/CF)
                                                                       │
                                                                       ▼
                                                                 12 (Fmt:Polish)
                                                                       │
                                                                       ▼
                                                                 13 (Golden Tests)

                           14 (Grammar docs) — parallel with 3–7
```

## Risks & Mitigations
- Ambiguous grammar → keep precedence small; add explicit separators; rely on required semicolons.
- Comment loss vs formatting → preserve doc comments; defer arbitrary inline comment retention to v0.2 if needed.
- Idempotency issues → property tests and incremental formatter passes.

## Acceptance Criteria
- All examples in docs/syntax.md and docs/vision.md parse and format; `aic check` passes on them.
- `aic fmt` is idempotent on corpus; disagreements reported with E0014.
- JSON diagnostics validated against schema; codes are documented.
- Token/char metric report produced; baseline recorded in repo.
